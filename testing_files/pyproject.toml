[tool.poetry]
name = "ml-service"
version = "0.1.0"
description = "AI inference service"

[tool.poetry.dependencies]
python = "^3.11"
openai = "^1.12.0"
langchain = "^0.1.5"
torch = {version = ">=2.0", optional = true}
scikit-learn = "^1.4.0"
flask = "^3.0.0"
